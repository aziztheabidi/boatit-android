package com.boatit.boatsharing.domain.core

/** Stable keys for [ErrorType.Validation.field] — use from UI instead of matching user-facing strings. */
object BusinessErrorCodes {
    const val PAY_BEFORE_CONFIRM = "pay_before_confirm"
}
